import { Navbar } from "nextra-theme-docs";

export const NavbarComponent = () => {
    return (
        <Navbar
            projectLink="https://github.com/Pixelartui"
            logo={<b>PixelArtUI</b>}
        />
    );
};
